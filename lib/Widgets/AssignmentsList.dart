import 'package:flutter/material.dart';

class AssignmentList extends StatelessWidget {
  AssignmentList(this.func);

  final void Function(
    String user,
    
  ) func;

  @override
  Widget build(BuildContext context) {
    return Container(

      child: InkWell(
        child: ,
      ),

    );
  }
}
